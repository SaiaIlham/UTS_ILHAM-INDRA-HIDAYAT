<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_formulir_pendaftaran"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="FORMULIR PENDAFTARAN"
        android:textStyle="bold"
        android:textSize="17sp"
        android:textAlignment="center"
        android:gravity="center"
        android:layout_marginTop="30dp" />

    <EditText
        android:id="@+id/et_nama_lengkap"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nama Lengkap"
        android:textStyle="bold"
        android:layout_marginTop="40dp"
        android:layout_below="@+id/tv_formulir_pendaftaran" />
    <EditText
        android:id="@+id/et_nomor_pendaftaran"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nomor Pendaftaran"
        android:textStyle="bold"
        android:layout_below="@+id/et_nama_lengkap"/>

    <Spinner
        android:id="@+id/sp_jalur_pendaftaran"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/et_nomor_pendaftaran"
        android:entries="@array/arr_jenis_pendaftaran"/>

    <CheckBox
        android:id="@+id/cbx_konfirmasi_daftar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Konfirmasi Pendaftaran"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/sp_jalur_pendaftaran" />

    <Button
        android:id="@+id/btn_daftar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="daftar"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/cbx_konfirmasi_daftar" />

</RelativeLayout>